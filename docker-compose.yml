services:
  db:
    image: 4training/mysql
    volumes:
      - vol-db:/var/lib/mysql
    ports:
      - "3306:3306"

  mediawiki:
    build:
      context: .
    ports:
      - "8082:80"
    volumes:
      - vol-www:/var/www/html/mediawiki
      - .:/src

volumes:
  vol-www:
  vol-db:
